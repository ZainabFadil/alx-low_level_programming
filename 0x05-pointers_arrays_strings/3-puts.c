#include <stdio.h>
#include "main.h"

/**
 * _puts - prints the given string followed by new line
 * @str: string argument
 * Return: void
 */
void _puts(char *str)
{
	/*the function puts print the string */
	puts(str);
}

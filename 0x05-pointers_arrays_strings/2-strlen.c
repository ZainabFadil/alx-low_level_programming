#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - function to get the length of a string
 * @s: is the parameter of type array of char
 * Return: the length of the string of type integer
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}

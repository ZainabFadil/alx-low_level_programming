#include <stdio.h>

/**
 * main - prints phrase and the starting point
 * Return: 0 in Success
 */
int main(void)
{
	/* puts ascii code */
	int ch;

	for (ch = 48; ch < 58; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
